rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules allow anyone with your project ID to read and write to your database.
      // After testing, lock these down to prevent unauthorized access.
      allow read, write: if true;
    }
  }
}
